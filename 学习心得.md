# 我的 AI 学习之路

> 从"比零基础多一点"到做出 10 个能用的工具（含 3 个 AI 工具）—— 一份真实的转行记录

## 我是谁

电子信息工程专业出身，学过一点 C 语言，但对 Web 开发、Python 完全陌生。为了往 AI 方向转行，我没有去刷教程、背概念，而是用了一个笨办法：**直接做能用的工具，边做边学**。

这个办法有个好处——每做完一个，手里就多一个能点开、能演示的东西。学到的知识不再是"看过"，而是"用过"。

## 我的作品（10 个工具 + 1 个首页）

| 作品 | 是什么 | 主要学到 |
|------|--------|---------|
| 🔍 文本提取工具 | 从一堆文字里捞出手机号、邮箱、身份证等 12 类信息 | 正则 `match`（提取） |
| 🧹 文本清洗工具 | 批量去空格、删空行、去重复、手机号打码 | 正则 `replace`（替换） |
| ✅ 格式验证器 | 判断输入是手机号还是邮箱，还能解码身份证、测密码强度 | 正则 `test` + 数据驱动查表 |
| 📊 文本统计工具 | 统计中英文、标点、段落、词频，还能检查字数超没超 | 对象计数字典 + 排序 |
| 📱 二维码生成器 | 网址 / WiFi / 名片生成二维码，扫码即用 | 调用开源库 + 解决中文编码 |
| 📦 数据格式转换器 | CSV ↔ JSON 互转、读 Excel、导出 PDF | JSON 解析 + 错误处理 |
| 🔄 **文件格式转换器（后端）** | Word / PDF / Excel 三种格式互转 | **Python + Flask 后端** |
| ✨ **AI 总结提炼工具** | 粘贴文章一键总结，可调形式/详细/语气 | **提示词设计** |
| 🌐 **AI 翻译工具** | 中英互译，可选风格、附词汇解释 | 角色设定 + 输出格式控制 |
| 💬 **AI 聊天助手** | 像 ChatGPT 一样多轮对话 | **调用大模型 + 多轮对话** |

## 我走过的三个阶段

### 阶段一：前端三件套 + 正则
第一个工具（文本提取）只会用最基础的 HTML + JavaScript。在这一阶段，我把「正则表达式」这个最唬人的概念吃透了——其实就三个动作：**提取（match）、替换（replace）、判断（test）**。搞懂这三个，文本类的活基本都能干。

### 阶段二：从"抄"到"改"到"会"
后面的工具越做越顺，因为我发现了规律：**大多数功能 = 数组处理 + 对象计数 + 调用现成的库**。遇到不会的，先查库有没有现成方案（比如二维码直接调 `qrcode` 库），而不是自己硬写。这个"知道去哪找轮子"的能力，比背 API 重要得多。

### 阶段三：跨过"后端"这道坎
最让我头疼的是"后端和前端到底有啥区别"——纠结了好久。直到做出这个文件转换器才彻底想通：

> **前端 = 浏览器里跑的东西，负责"长什么样"；后端 = 电脑/服务器上跑的程序，负责"真正干活"。**

我以前的工具，格式转换这种"重活"其实干不了（浏览器权限有限）；有了 Python 后端，才能指挥 LibreOffice 去转 PDF、用 pandas 读 Excel。跨过这一步，我才算真正摸到了"开发"的门。

### 阶段四：调用大模型 API，做出真正的 AI 工具
有了后端打底，我把目光转向「AI 工具」——这次不是自己写规则，而是调用 DeepSeek 大模型。连着做了三个（总结、翻译、聊天），我搞懂了几件重要的事：

- **提示词就是给大模型的指令**。总结工具里加「形式/详细/语气」选项，翻译里做「角色设定」和「输出格式控制」，选的越清楚，结果越合意。
- **多轮对话的秘密**：把历史聊天记录每次都完整发回，大模型就"记得"之前说过的话。
- **安全规范**：学会了用 `.env` 环境变量管理密钥，不把 key 硬编码进代码。

## 我踩过的坑（也是收获）

- **中文乱码**：二维码、CSV 都栽过中文乱码，最后搞懂了 UTF-8 编码（一个汉字 = 3 个字节）。
- **选库要验证**：二维码试了三四个库才找到能正确支持中文的，学会了"先验证再动手"。
- **环境配置**：装 Python、配镜像源、装 LibreOffice……这些"脏活"也是开发的一部分。
- **变量名冲突**：聊天助手报 `history.push is not a function`，原来是 `history` 是浏览器内置保留字。顺着 F12 控制台的报错一路排查，从此不再怕报错了。

## 我的心得（说给自己，也分享给同样转行的你）

1. **作品导向 > 刷课导向**。一个能打开的工具，胜过一百页没实践的笔记。
2. **AI 是加速器，不是替身**。我用 AI 帮忙写代码、解释概念，但每一行我都追问"为什么"，自己动手跑、亲手改，才真正变成自己的。
3. **敢踩坑才有成长**。中文乱码、CDN 被墙、库不兼容……每次卡住再解决，都是最扎实的学习。

---

*这不是终点。下一步，我想把这些工具部署上线，让别人也能通过网址访问；或者探索 RAG，让 AI 读懂我自己的文档。*
